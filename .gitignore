# local DB cache
*.db
*.db-shm
*.db-wal

# downloaded/indexed reference data
/data/

# local config (copy from config.example.toml)
config.toml

# build output
/varhub
/bin/
/dist/

# local scratch
/tmp/

# go workspace (machine-local)
go.work
go.work.sum

# Test pollution guards. Both of these were committed once: a source's location
# overlay written beside a fixture manifest, and a URL mistaken for a path.
*.locations.toml
/http:/
/https:/
